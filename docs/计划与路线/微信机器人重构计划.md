# 微信个人 Agent 通道重构计划：wechatbot.dev + 用户级会话闭环

> 重写日期：2026-05-10
> 状态：v0.4（2026-05-10 三次审阅后锁定统一 IM 可插拔接口）。本文件取代旧版 `微信机器人重构计划.md`。
>
> **已锁定的关键决策（2026-05-10）：**
> 1. **产品定位**：销售 / 老板个人助手——付费个人或企业采购后分发个人使用。MVP 真实场景是"1:1 客户咨询补充"。企业全员场景走企微通道。
> 2. **企微平行**：本计划范围不变（个人微信）；企微通道补齐作为**平行优先项**，单独立项。
> 3. **Phase -1 SDK Spike**：先实测官方 wechatbot.dev SDK，再删除旧协议。Spike 不通过时暂停计划，不能先把回退路径删掉。
> 4. **Phase 0 旧协议**：Spike 通过后硬删除 `wechatpadpro/` `wechaty/`，grep gate 严格执行。**接受的代价**：one-way door，wechatbot.dev SLA 出问题无回退路径，必须靠 Phase 4 上线 hardening + canary 兜底。
> 5. **统一 IM scope 契约**：不得为 wechatbot 开绕过 Router 的 Master / tools / session 发送专线。wechatbot 必须作为 `ChannelPlugin` 接入统一 Router；入站用 `channel.InboundMessage.OwnerUserID`，出站跨包 DTO 用 `imctx.SendRequest.OwnerUserID`，插件发送用 `channel.OutboundMessage.OwnerUserID`。`TenantKey=OwnerUserID` 参与 `BuildSessionID`。Router 通过 `OwnerUserResolver` 注入 auth user；禁止用 context value 传 owner，禁止修改 `enrichCtx` 签名。
>
> **群聊能力硬事实（SDK 实证）：**
> - 当前官方 Go SDK / iLink 文档未暴露稳定群聊 RoomID/ChatroomID/GroupID 入口
> - 当前发送模型围绕 `userID` 与 prior `context_token`，未提供稳定 SendToRoom API
> - 因此不要在 MVP 承诺群聊、@、群成员管理或任意群推送
> - 因此 MVP 仅支持 1:1 联系人会话，群聊延后到上游 SDK 支持后另议

---

## 0. 结论先行

这次重构不是“把 WeChatPadPro / Wechaty 换成 wechatbot.dev”这么简单。真正要交付的是一个用户级产品闭环：

1. 每个登录用户连接自己的个人微信账号。
2. 微信联系人发来的消息进入该用户自己的 Agent 会话。
3. 用户可以在 Web 端继续同一个微信会话，并把消息真实发回微信。
4. 系统能清楚解释“未登录、已掉线、无可用 context token、跨用户访问”等失败原因。
5. WeChatPadPro (iPad) 和 Wechaty (gRPC) 必须从产品与代码路径中清空；个人微信只允许走官方 wechatbot.dev / iLink SDK。

MVP 的关键不是联系人管理、群聊管理或任意冷启动推送，而是“一人一扫码、一联系人一会话、微信与 Web 双向可用”。

---

## 1. 旧计划必须修正的问题

| 旧计划假设 | 当前问题 | 新计划修正 |
| --- | --- | --- |
| `enrichCtx(ctx, wxid, "wechatbot")` 可把微信用户映射成系统用户 | `wxid` 是外部联系人，不是 agents-hive 登录用户；这会把外部联系人错当系统用户 | 系统用户来自 `BotInstance.OwnerUserID`，外部联系人只作为 `PeerWxid` |
| Session ID 写成 `im-wechatbot-{wxid}` | 当前 IM 会话必须通过 `imctx.BuildSessionID(platform, tenantKey, chatID)` 生成 | 新增 `PlatformWeChatBot = "wechatbot"`，使用 `BuildSessionID("wechatbot", ownerUserID, peerWxid)` |
| `/api/wechat/*` | 当前 API 统一使用 `/api/v1/*` | 新接口统一放在 `/api/v1/wechat/*` |
| WeChat 设置页还需要新建 | `frontend/src/pages/Settings.tsx` 已存在，但尚未在 `App.tsx` 注册路由 | 复用并扩展 `/settings`，不要重新发明设置页 |
| IM session 会出现在普通会话列表 | `internal/api/session_handlers.go` 当前显式过滤 `strings.HasPrefix(session.ID, "im-")` | Phase 2 必须改 Session 列表策略，让微信会话以受控方式展示 |
| Web 端向微信会话发消息天然可用 | `POST /api/v1/sessions/{id}/messages` 当前只进入 Master / Agent，没有 IM 出站分支 | 对 `wechatbot` IM session 增加 Web 出站路径 |
| `send_im_message` 可以直接支持用户级微信发送 | `internal/tools/sendim.go` 的 `SendMessage(ctx, platform, chatID, content)` 缺少 scope 维度 | 工具层走统一 IM send request；wechatbot 的 owner 来自 auth context，禁止让 Agent 传任意 `owner_user_id` |
| 保留 WeChatPadPro / Wechaty 作为回退 | 当前实现是非官方协议，会继续污染配置、工具、前端和路由心智 | Phase 0 先清空旧协议入口与实现，个人微信唯一官方路径是 wechatbot.dev |
| 数据库使用 `UUID REFERENCES auth.users(id)` | 当前仓库用户表是 `users(id TEXT)`，没有 `auth.users` schema | 新迁移必须使用 `TEXT REFERENCES users(id)` |
| 任意主动发到任意 wxid | wechatbot Go SDK 的 `Send` / `SendMedia` 需要 prior context；冷启动推送不是 MVP 能承诺的能力 | 产品文案和接口都明确“仅可发送到已有上下文的会话” |

---

## 2. 当前代码事实

以下代码事实是本计划的约束，不是建议。

| 位置 | 当前事实 | 对计划的影响 |
| --- | --- | --- |
| `internal/imctx/session.go` | `BuildSessionID(platform, tenantKey, chatID)` 是 IM session_id 的唯一构造入口，格式为 `im-{platform}-{tenantKey}-{chatID}` | 不能手拼 `im-wechatbot-*` |
| `internal/imctx/context.go` | `Platform` 常量与 `internal/channel/types.go` 需要同步 | 新增 `wechatbot` 必须两处同步 |
| `internal/channel/plugin.go` | `ChannelPlugin.Send(ctx, OutboundMessage)` 已是统一插件接口 | wechatbot 必须实现插件接口；API / Master / tools 不直接调用 wechatbot manager |
| `internal/channel/router.go` | 目前入站消息会用 `msg.SenderID` 调 `enrichCtx` 查系统用户 | wechatbot 不能沿用默认逻辑；必须在 `InboundMessage.OwnerUserID` 上显式携带 owner |
| `internal/channel/router.go` | `TenantKey` 为空会回退 `default` | 微信个人通道必须把 `OwnerUserID` 放入 tenant 维度，不能依赖 `default` |
| `internal/channel/router.go` | `bindings` 当前 key 是 `platform:chatID` | 必须改成 `platform:tenantKey:chatID`，否则不同 owner 的同一 `PeerWxid` 会串会话 |
| `internal/master/session_loop.go` / `public_api.go` | session 的 `UserID` 来自 auth context | wechatbot 入站必须注入 owner user，才能让 session 归属正确 |
| `internal/master/user_access.go` | 开启 auth 后用户只能访问自己的 session 或遗留无主 session | 微信 session 必须持久化 `sessions.user_id = OwnerUserID` |
| `internal/api/session_handlers.go` | `GET /api/v1/sessions` 当前过滤所有 `im-` session | 微信会话展示需要改列表策略 |
| `internal/tools/sendim.go` | 工具只传 `platform/chat_id/content` | 需要通过统一 send request 补 scope；owner 只能从当前 auth user 派生 |
| `frontend/src/pages/Settings.tsx` | 普通用户设置页已存在，含主题与语言 | 微信连接入口应加到这里 |
| `frontend/src/App.tsx` | 仅注册 `/`、`/sessions/:id`、`/replay`、`/guide`；没有 `/settings` | Phase 2 要注册 `/settings` |
| `frontend/src/pages/AdminSettings.tsx` | integrations tab 仍渲染旧 `WeChatSettings` | Phase 0 删除旧微信配置入口；Admin 后续只保留 wechatbot 全局开关 |
| `frontend/src/components/settings/WeChatSettings.tsx` | 仍是 WeChatPadPro / Wechaty 管理配置 | 删除旧组件；新个人微信连接组件另建 |
| `internal/bootstrap/server.go` / `internal/bootstrap/helpers.go` | 仍初始化 wechaty / wechatpadpro | Phase 0 移除旧协议初始化、import、注册 |
| `internal/api/config_handlers.go` | 当前微信配置 API 仍是旧协议状态 | Phase 0 移除旧协议配置接口；wechatbot 用户级 API 单独建设 |

---

## 3. 产品北极星

### 3.1 目标用户

目标用户不是系统管理员，而是每一个已登录 agents-hive 的普通用户：

- 他希望让自己的微信联系人可以直接找他的 Agent。
- 他希望在 Web 端看到这些微信联系人会话。
- 他希望必要时从 Web 端接管回复，而不是只能等 Agent 自动回复。
- 他不应该理解 WeChatPadPro、Wechaty、tenant、chatID、context token 这些实现细节。

### 3.2 第一版 10 分体验

1. 用户打开 `/settings`。
2. 点击“连接微信”，看到二维码。
3. 手机微信扫码并确认。
4. 页面显示当前连接账号、在线状态、最近微信会话数。
5. 某个微信联系人发消息。
6. 左侧会话列表出现一个带“微信”标识的新会话。
7. 用户打开会话，能看到微信消息、Agent 回复和来源标记。
8. 用户在 Web 输入回复。
9. 如果该联系人有有效 context，消息发回微信；如果没有，UI 明确提示“该联系人暂无可发送上下文，请先让对方在微信中发一条消息”。

### 3.3 成功指标

| 指标 | MVP 目标 |
| --- | --- |
| 扫码成功到在线状态 | 30 秒内完成，失败给出可操作错误 |
| 微信入站到 Web 会话可见 | 5 秒内可见 |
| 同一联系人会话稳定性 | 同一 owner + peer 总是落到同一 session |
| Web 发回微信成功率 | 对已有 context 的会话，成功率可观测 |
| 跨用户隔离 | 0 容忍：用户 A 不能看到或发送用户 B 的微信会话 |
| 官方协议纯度 | 代码与 UI 不再提供 WeChatPadPro / Wechaty 入口 |

---

## 4. 官方 SDK 约束

参考：

- GitHub: `https://github.com/corespeed-io/wechatbot`
- Go SDK 文档: `https://www.wechatbot.dev/zh/golang`
- iLink 协议文档: `https://www.wechatbot.dev/zh/protocol`

截至 2026-05-10 核对，Go SDK 关键能力如下：

| 能力 | 对产品/工程的约束 |
| --- | --- |
| `go get github.com/corespeed-io/wechatbot/golang` | 依赖升级要检查当前 `go.mod` 与构建环境 |
| `bot.Login(ctx, force)` 返回 credentials，包含账号身份 | 登录成功后用 SDK 返回的账号 ID 作为 `OwnerAccountID` |
| `Options` 支持 `CredPath`、`OnQRURL`、`OnScanned`、`OnExpired`、`OnError` | 后端需要事件桥接到 WebSocket / SSE |
| `bot.OnMessage(func(*IncomingMessage))` | 入站消息由 BotInstance 转成内部会话消息 |
| `bot.Reply(ctx, msg, text)` | 自动回复路径可以优先使用 Reply，天然绑定 incoming context |
| `bot.Send(ctx, userID, text)` | 主动发送需要 prior context，不能承诺给任意 wxid 冷启动推送 |
| `context_token` 自动生命周期管理并可持久化 | 会话表要记录“是否可 Web 出站”，但不要自己伪造 token |
| 支持图片、文件、语音、视频 | MVP 可先文本，媒体作为 Phase 3；但数据结构要预留 |
| 会话过期 `-14` 自动触发重新登录 | 产品状态需要区分 `online`、`relogin_required`、`recovering`、`offline` |
| **群聊** | 当前官方 Go SDK / iLink 文档未暴露稳定群聊入口。MVP 仅 1:1 联系人；群聊必须等 Phase -1 Spike 或上游文档确认后另议。 |
| **@ 提及 / 消息撤回 / 群成员管理** | 当前官方文档未提供可依赖能力，MVP 不承诺。 |

---

## 5. 身份模型

这是整个重构的核心。任何实现绕开它，都会造成串号、越权或无法回发。

### 5.1 三个身份

| 字段 | 含义 | 示例 | 归属 |
| --- | --- | --- | --- |
| `OwnerUserID` | agents-hive 登录用户 ID | `user_abc` | 系统内部用户 |
| `OwnerAccountID` | 扫码登录成功的微信账号 ID | SDK credentials 中的 account id | 用户自己的微信号 |
| `PeerWxid` | 给该微信号发消息的外部联系人 ID | `wxid_xxx` | 外部联系人 |

必须禁止的混淆：

- `PeerWxid` 不是系统用户 ID。
- `OwnerAccountID` 不是 agents-hive 用户 ID。
- `OwnerUserID` 不能从入站消息 sender 反查，必须来自 BotInstance。

### 5.2 绑定关系

`OwnerUserID` 与 `OwnerAccountID` 是“该系统用户连接了哪个微信号”的绑定。它用于：

- 登录状态展示。
- 凭证目录隔离。
- 防止同一微信账号被多个系统用户重复绑定。
- 入站消息创建 session 时注入系统用户上下文。

`PeerWxid` 与 `OwnerUserID` 是“该用户与哪个外部联系人有会话”的关系。它用于：

- 构造稳定 session。
- 展示联系人昵称、头像、最近消息。
- 判断 Web 端是否可发回微信。

---

## 6. 会话模型

### 6.1 Platform 常量

新增常量，必须同步两处：

```go
// internal/channel/types.go
PlatformWeChatBot Platform = "wechatbot"

// internal/imctx/context.go
PlatformWeChatBot Platform = "wechatbot"
```

旧协议常量必须删除：

- `wechat-wechaty`
- `wechat-wechatpadpro`

删除要求：

- `internal/channel/types.go` 不再暴露旧微信 Platform。
- `internal/imctx/context.go` 不再暴露旧微信 Platform。
- `internal/tools/sendim.go` 的 platform enum 不再包含旧微信协议。
- 任何旧协议测试、文案、配置示例同步清理。

### 6.2 Session ID

MVP 采用现有 IM session 规范：

```go
sessionID, err := imctx.BuildSessionID(
    imctx.PlatformWeChatBot,
    ownerUserID,
    peerWxid,
)
```

语义：

- `platform = wechatbot`
- `tenantKey = OwnerUserID`
- `chatID = PeerWxid`

这样同一个外部联系人给不同系统用户的微信号发消息时，会落到不同 session：

```text
im-wechatbot-user_a-wxid_peer
im-wechatbot-user_b-wxid_peer
```

禁止：

```text
im-wechatbot-{wxid}
```

原因：

- 没有 owner 维度，会跨用户串会话。
- 绕过 `BuildSessionID`，违反现有 IM 规范。
- 无法和现有 router / master / access check 一致。

### 6.3 Session 归属

微信入站创建 session 时，必须让 `sessions.user_id = OwnerUserID`。

确定做法：

1. `BotInstance` 收到 SDK `IncomingMessage`。
2. `BotInstance` 构造统一 `channel.InboundMessage`，必须设置 `OwnerUserID=BotInstance.OwnerUserID`、`TenantKey=OwnerUserID`、`ChatID=PeerWxid`、`SenderID=PeerWxid`。
3. `BotInstance` 调用现有 `router.HandleMessage(ctx, msg)`，不得绕过 Router 直连 Master。
4. Router 识别 user-scoped 插件后，用 `OwnerUserResolver` 按 `OwnerUserID` 加载系统用户，调用 `auth.WithUser(ctx, user)`，并写入 `IMContextValue.InternalUserID`。
5. Router 使用 `BuildSessionID(platform, msg.TenantKey, msg.ChatID)` 得到 session，进入 Master 前确保 session owner 被正确写入。

禁止事项：

- 不新增 `HandleMessageWithOwnerContext` 这种旁路入口。
- 不用自定义 context value 携带 owner。
- 不修改 `enrichCtx` 签名。`enrichCtx(ctx, externalID, provider)` 继续只服务飞书 / 钉钉 / 企微这类 tenant-scoped 企业 IM；wechatbot 的 `PeerWxid` 永远不进入 `enrichCtx`。

### 6.4 Binding Key

当前 Router 的内存绑定 key 是 `platform:chatID`。这在 wechatbot 下会串号：同一个 `PeerWxid` 可能同时给用户 A 和用户 B 的个人微信发消息。

确定改造：

- `channel.Binding` 增加 `TenantKey string`。
- `Bind` / `LookupSession` / `Unbind` 的内部 key 统一改为 `platform:tenantKey:chatID`。
- Router 在入站时先归一化 `tenantKey`：wechatbot 必须是 `OwnerUserID`；tenant-scoped 企业 IM 允许继续用平台 tenant/corp/open tenant，空值才回退 `default`。
- 不保留 `platform:chatID` 新写入路径；当前 binding 是进程内内存状态，不需要做持久化迁移。

---

## 7. 数据模型

wechatbot 相关迁移必须 additive。旧协议配置和凭证清理可以在 Phase 0 进行，但不要误删新 wechatbot 数据。

### 7.1 用户外部身份绑定

旧计划里的 `UUID REFERENCES auth.users(id)` 是错的。当前仓库用户表为 `users(id TEXT)`。

```sql
CREATE TABLE IF NOT EXISTS user_external_ids (
    id BIGSERIAL PRIMARY KEY,
    user_id TEXT NOT NULL REFERENCES users(id) ON DELETE CASCADE,
    provider_type TEXT NOT NULL,
    external_id TEXT NOT NULL,
    display_name TEXT NOT NULL DEFAULT '',
    avatar_url TEXT NOT NULL DEFAULT '',
    metadata JSONB NOT NULL DEFAULT '{}'::jsonb,
    created_at TIMESTAMPTZ NOT NULL DEFAULT NOW(),
    updated_at TIMESTAMPTZ NOT NULL DEFAULT NOW(),
    UNIQUE (provider_type, external_id),
    UNIQUE (user_id, provider_type)
);

CREATE INDEX IF NOT EXISTS idx_user_external_ids_user_provider
    ON user_external_ids(user_id, provider_type);
```

说明：

- `provider_type = "wechatbot"`。
- `external_id = OwnerAccountID`。
- `UNIQUE(provider_type, external_id)` 防止同一个微信号被多个系统用户绑定。
- `UNIQUE(user_id, provider_type)` 限制 MVP 一个用户只连一个微信账号；多账号支持以后单独设计。
- 现有 `auth.GetUserByExternalIDAndProviderType` 依赖 `users` + `auth_providers` 行为，不能粗暴替换；如要引入 `user_external_ids`，必须保持原登录 provider 查询兼容。

### 7.2 微信会话表

```sql
CREATE TABLE IF NOT EXISTS wechat_conversations (
    id BIGSERIAL PRIMARY KEY,
    owner_user_id TEXT NOT NULL REFERENCES users(id) ON DELETE CASCADE,
    owner_account_id TEXT NOT NULL,
    peer_wxid TEXT NOT NULL,
    session_id TEXT NOT NULL,
    peer_nickname TEXT NOT NULL DEFAULT '',
    peer_avatar_url TEXT NOT NULL DEFAULT '',
    chat_type TEXT NOT NULL DEFAULT 'direct',
    last_message_preview TEXT NOT NULL DEFAULT '',
    last_message_at TIMESTAMPTZ,
    can_send BOOLEAN NOT NULL DEFAULT FALSE,
    send_state TEXT NOT NULL DEFAULT 'unknown',
    metadata JSONB NOT NULL DEFAULT '{}'::jsonb,
    created_at TIMESTAMPTZ NOT NULL DEFAULT NOW(),
    updated_at TIMESTAMPTZ NOT NULL DEFAULT NOW(),
    UNIQUE (owner_user_id, peer_wxid),
    UNIQUE (session_id)
);

CREATE INDEX IF NOT EXISTS idx_wechat_conversations_owner_last
    ON wechat_conversations(owner_user_id, last_message_at DESC NULLS LAST);
```

`can_send` 的含义不是“理论上能发送到任何 wxid”，而是“SDK 当前有足够上下文支持对该 peer 出站”。实现可根据 SDK context token 状态、最近入站消息或 Send 失败结果更新。

### 7.3 凭证存储

目录建议：

```text
{DataDir}/wechatbot/users/{OwnerUserID}/credentials.json
{DataDir}/wechatbot/users/{OwnerUserID}/state.json
```

要求：

- 目录权限 `0700`。
- 文件权限 `0600`。
- 日志禁止打印 credentials 内容。
- logout 时优先调用 SDK logout/stop，再清理本地凭证；清理失败要展示可操作错误。

---

## 8. 后端架构

### 8.1 模块边界

建议新增：

```text
internal/channel/wechatbot/backend.go
internal/channel/wechatbot/instance.go
internal/channel/wechatbot/store.go
internal/channel/wechatbot/events.go
internal/channel/wechatbot/plugin.go
internal/channel/wechatbot/registry.go
internal/api/wechat_handlers.go
```

职责：

| 模块 | 职责 |
| --- | --- |
| `wechatbot.Backend` | SDK adapter：Login、Run、Stop、Reply、Send、status |
| `BotInstance` | 一个系统用户的一条微信连接，持有 OwnerUserID / OwnerAccountID / SDK Bot |
| `wechatbot.Plugin` | 实现 `channel.ChannelPlugin`，作为 wechatbot 进入统一 IM Router 的唯一插件入口 |
| `BotRegistry` | 插件内部管理 `map[OwnerUserID]*BotInstance`，只暴露给 `wechatbot.Plugin` 与登录生命周期服务，不给 Master / session API / tools 直连发送 |
| `ConversationStore` | 维护 `wechat_conversations` 与 session 映射 |
| `WeChatConnectionService` | `/api/v1/wechat/*` 登录 / 状态 / 事件 / logout 的窄接口；不提供发送能力 |
| `wechat_handlers.go` | 暴露 `/api/v1/wechat/*` 用户级 API，handler 依赖 `WeChatConnectionService` 接口 |

### 8.1.1 统一 IM 接口锁定

IM 必须是统一、可插拔接口，而不是为 wechatbot 硬耦合一套链路。

```go
// internal/channel/types.go
type InboundMessage struct {
    Platform    Platform
    TenantKey   string
    OwnerUserID string // 仅 user-scoped IM 必填；tenant-scoped 企业 IM 为空
    ChatID      string
    SenderID    string
    // ...
}

type OutboundMessage struct {
    Platform    Platform
    TenantKey   string
    OwnerUserID string // wechatbot 插件发送必填；企业 IM 为空
    ChatID      string
    Content     string
    // ...
}

// internal/imctx/send_request.go
// tools / API / Router 共用的跨包出站 DTO。放在 imctx，避免 tools -> channel ->
// master -> tools 的 import cycle。
type SendRequest struct {
    Platform    Platform
    TenantKey   string
    OwnerUserID string // wechatbot 必填；企业 IM 为空
    ChatID      string
    Content     string
    MsgType     string
    ReplyTo     string
}
```

Router 统一契约：

- `HandleMessage(ctx, InboundMessage)` 是唯一入站入口；不新增 wechatbot 专用 handler。
- `Router.SendMessage(ctx, imctx.SendRequest)` 是统一出站入口；Router 在包内转换成 `channel.OutboundMessage` 后调用插件 `Send`。
- `internal/tools/sendim.go` 的窄接口改成 `SendMessage(ctx, imctx.SendRequest)`；工具包不得 import `internal/channel`，避免 import cycle。
- `OwnerUserID != ""` 表示 user-scoped IM。Router 通过 `OwnerUserResolver` 注入 auth user 并跳过 `enrichCtx`。
- `OwnerUserID == ""` 表示 tenant-scoped IM。Router 保持现有 `enrichCtx(ctx, SenderID, provider)` 行为。
- `wechatbot.Plugin.Send` 要求 `msg.OwnerUserID != ""` 且 `msg.TenantKey == msg.OwnerUserID`，再在插件内部按 owner 找 `BotInstance` 发送；飞书 / 钉钉 / 企微忽略 `OwnerUserID` 零值。
- session API / Master / tools 的发送路径只能依赖 Router 的统一入口；不得 import `internal/channel/wechatbot` 后直接调 `BotRegistry.SendByOwner`。
- `/api/v1/wechat/*` 登录生命周期 API 只能依赖 `WeChatConnectionService` 窄接口，不允许把发送方法挂到该接口上。

### 8.2 入站消息流程

```text
wechatbot SDK OnMessage
  -> BotInstance.handleIncoming(ownerUserID, ownerAccountID, msg)
  -> upsert wechat_conversations(ownerUserID, peerWxid, sessionID)
  -> build channel.InboundMessage{OwnerUserID, TenantKey=OwnerUserID, ChatID=PeerWxid}
  -> Router.HandleMessage(ctx, inbound)
  -> Router.OwnerUserResolver(ownerUserID) + auth.WithUser
  -> Master / channel processor
  -> Agent response
  -> Router.SendMessage(imctx.SendRequest{OwnerUserID, TenantKey=OwnerUserID, ChatID=PeerWxid})
  -> wechatbot.Plugin finds BotInstance and Reply/Send when allowed
  -> persist and broadcast to Web
```

关键要求：

- `wechatbot.Plugin` 和 `BotInstance` 必须是 owner-aware，但 owner 只能作为统一 IM scope 流经 Router。
- 不得把 `PeerWxid` 传给 `enrichCtx` 当系统用户查。
- `Router` 新增 `SetOwnerUserResolver(func(ctx context.Context, userID string) (*auth.User, error))`，bootstrap 注入 `authEngine.GetUserByIDCached`。
- `processMessageImpl` 中 `msg.OwnerUserID != ""` 时：校验 `msg.TenantKey == msg.OwnerUserID`，解析 active user，注入 `auth.WithUser`，设置 `IMContextValue.InternalUserID`，并跳过 `enrichCtx`。
- `processMessageImpl` 中 `msg.OwnerUserID == ""` 时：保持飞书 / 钉钉 / 企微现有 `enrichCtx(ctx, msg.SenderID, provider)` 行为。
- 入站消息创建 session 前必须完成 owner 注入，否则 auth 开启时 session 会变成无主或越权不可访问。
- 保留 router 现有 dedup、debounce、BuildSessionID、renderer、ack、retry_queue 约束。
- Binding key 必须使用 `platform:tenantKey:chatID`，否则 owner 维度会被 `LookupSession` 绕开。

### 8.3 Web 出站流程

当前 `POST /api/v1/sessions/{id}/messages` 只会把用户输入交给 Master。微信会话需要一个分支：

```text
POST /api/v1/sessions/{sessionID}/messages
  -> check session owner
  -> detect session is im-wechatbot
  -> lookup wechat_conversations by sessionID + ownerUserID
  -> Router.SendMessage(imctx.SendRequest{Platform: wechatbot, OwnerUserID: currentUserID, TenantKey: currentUserID, ChatID: peerWxid, Content: content})
  -> wechatbot.Plugin.Send -> BotRegistry -> BotInstance
  -> append message metadata: source=web, im_outbound=true, platform=wechatbot
  -> broadcast message to session
```

失败语义：

| 场景 | HTTP | 用户提示 |
| --- | --- | --- |
| 未连接微信 | `409` | “请先在设置页连接微信” |
| Bot 离线 | `409` | “微信连接已断开，请重新扫码” |
| 无 context token / 不可发送 | `409` | “该联系人暂无可发送上下文，请先让对方在微信中发一条消息” |
| session 不属于当前用户 | `403` | “无权访问该会话” |
| SDK 临时失败 | `502` | “微信发送失败，请稍后重试” |

### 8.4 主动工具发送

`send_im_message` 不能继续只靠 `platform + chat_id` 发送微信消息。

**改造方向（v0.4 锁定：统一 `imctx.SendRequest`，不暴露 owner 参数给 Agent）：**

```go
type IMRouter interface {
    SendMessage(ctx context.Context, req imctx.SendRequest) error
}
```

调用约定：

- `send_im_message` 的 JSON schema 仍只允许 `platform/chat_id/content`，不增加 `owner_user_id` 字段。
- 工具 handler 从 `auth.UserFrom(ctx)` 派生当前用户。
- 当 `platform=wechatbot`：handler 查 `wechat_conversations(session/chat_id, currentUserID)` 后填 `imctx.SendRequest.OwnerUserID=currentUserID` 与 `TenantKey=currentUserID`。
- 当 `platform=feishu/dingtalk/wecom`：handler 保持 `OwnerUserID=""`，按平台现有 `TenantKey/chatID` 发送。

工具层要求：

- 从 tool context / auth context 解析当前系统用户。
- 禁止 Agent 传入任意 `owner_user_id`。
- 对 `wechatbot` 平台，必须验证 `wechat_conversations.owner_user_id = 当前用户`。
- 无 context token 时返回明确 unsupported/temporarily unavailable，不要静默失败。
- 现有 6 个 WeChat ops 工具在 wechatbot 下先返回“不支持该协议”，避免误导用户。

---

## 9. API 设计

所有接口均为登录用户级接口，路径统一使用 `/api/v1/wechat/*`。

| 方法 | 路径 | 用途 |
| --- | --- | --- |
| `GET` | `/api/v1/wechat/status` | 当前用户微信连接状态 |
| `POST` | `/api/v1/wechat/login` | 创建/刷新扫码登录流程 |
| `GET` | `/api/v1/wechat/events` | 登录二维码、扫码、过期、在线/掉线事件流 |
| `POST` | `/api/v1/wechat/logout` | 停止 Bot 并解绑本地凭证 |
| `POST` | `/api/v1/wechat/relogin` | 强制重新扫码 |
| `GET` | `/api/v1/wechat/conversations` | 当前用户微信会话列表 |

MVP 不提供 `/api/v1/wechat/conversations/{peerWxid}/messages` 直发接口。Web 出站统一走 `POST /api/v1/sessions/{id}/messages`，再由 session handler 构造 `imctx.SendRequest` 进入 Router，避免绕过 session owner 校验、消息记录和统一 IM 出站链路。

### 9.1 状态响应

```json
{
  "enabled": true,
  "status": "online",
  "owner_account_id": "wx_account_xxx",
  "display_name": "微信昵称",
  "avatar_url": "",
  "conversation_count": 12,
  "last_connected_at": "2026-05-10T12:00:00Z",
  "error": ""
}
```

`status` 建议枚举：

- `disabled`
- `not_connected`
- `waiting_qr_scan`
- `scanned`
- `online`
- `recovering`
- `relogin_required`
- `offline`
- `error`

### 9.2 事件流安全

浏览器 WebSocket 通常不能设置 `Authorization` header。当前仓库 `/api/v1/ws` 已有特殊 token 校验思路；新的 `/api/v1/wechat/events` 必须复用同等级校验，不允许因为是 WebSocket / SSE 就绕过 auth。

要求：

- 事件 hub 按 `OwnerUserID` 分区。
- 用户 A 只能收到用户 A 的 QR / status 事件。
- token 放 query 参数时，日志必须脱敏。
- 断线重连不应重复创建多个 BotInstance。

---

## 10. 前端产品设计

### 10.1 路由与入口

当前 `Settings.tsx` 已存在但未注册。Phase 2 必须：

- 在 `frontend/src/App.tsx` 增加 `/settings` 路由。
- 在主侧边栏增加 Settings 入口。
- 普通用户在 `/settings` 连接微信。
- Admin 的 `/admin/settings` 只保留 wechatbot 全局开关和运行配置，不再出现 WeChatPadPro / Wechaty。

不建议新增 `/wechat` 作为第一入口。微信连接是用户设置的一部分；联系人会话则应出现在现有 session 体验里。

### 10.2 设置页模块

新增组件建议：

```text
frontend/src/components/settings/WeChatConnectionPanel.tsx
frontend/src/components/settings/WeChatQRCode.tsx
frontend/src/components/settings/WeChatStatusBadge.tsx
frontend/src/hooks/useWechatConnection.ts
frontend/src/api/wechat.ts
```

页面结构：

1. 当前连接状态。
2. 连接/重新连接/退出登录按钮。
3. 二维码区域。
4. 最近微信会话摘要。
5. 错误与操作提示。

产品文案必须避免承诺“可主动发给任何联系人”。正确文案：

- “连接后，给你发过消息的微信联系人会出现在会话列表中。”
- “Web 端可回复已有上下文的微信会话。”

### 10.3 Session 列表

当前后端过滤所有 `im-` session。要让微信会话可见，需要做受控展示：

- 只展示当前用户自己的 `wechatbot` session。
- 显示“微信”来源标识。
- 标题优先使用 `peer_nickname`，缺失时使用脱敏 `PeerWxid`。
- 最近消息来自 `wechat_conversations.last_message_preview` 或 session 最新消息。
- 对非微信 IM session 是否展示，要另做产品判断，不在本计划顺手打开所有 IM session。

建议扩展 `SessionListItem`：

```json
{
  "id": "im-wechatbot-user_abc-wxid_xxx",
  "name": "张三",
  "source": "wechatbot",
  "source_label": "微信",
  "peer_avatar_url": "",
  "can_send": true
}
```

### 10.4 Chat 页面

对微信会话增加：

- 消息来源标识：微信 / Web / Agent。
- 出站失败状态。
- `can_send=false` 时输入框仍可显示，但发送按钮应给出明确提示。
- 媒体消息 Phase 3 再渲染，但 MVP 不应把媒体消息吞掉；至少展示“收到一条图片/语音/文件消息”占位。

---

## 11. 分阶段计划

### Phase -1：官方 SDK Spike

目标：先证明官方 wechatbot.dev SDK 能支撑 MVP，再进入不可逆的旧协议删除。Phase -1 不删旧代码、不改用户可见功能，只产出实测结论和最小样例。

- [ ] 拉取 `github.com/corespeed-io/wechatbot/golang`，跑通 hello world。
- [ ] 实测扫码登录耗时和二维码过期行为。
- [ ] 实测 `Login(ctx, force)` 返回 credentials 中可用于 `OwnerAccountID` 的稳定账号字段。
- [ ] 实测 `OnMessage` 回调字段：确认 1:1 sender 标识、昵称/头像可用性、是否有群聊字段。
- [ ] 实测 `Reply` 与 `Send`：有 prior context 时能发，缺 context 时记录具体错误码和错误类型。
- [ ] 实测媒体消息最小入站：图片/文件/语音/视频至少不 panic，确认 SDK 暴露字段。
- [ ] 实测多用户多 BotInstance 并发：凭证路径隔离、状态隔离、goroutine/panic 行为。

验收：

- Spike 结论写回本计划或单独 spike 报告。
- 若 `Send` 无 context 的错误不可识别，Web 出站必须只允许 `Reply` 场景或延后。
- 若 SDK 无法稳定拿到账号身份，暂停 Phase 0，重新设计 `OwnerAccountID`。

### Phase 0：官方协议纯化与基础约束

目标：在 Phase -1 通过后清空非官方微信协议，把不会变的契约落稳。完成后，仓库里个人微信只剩 wechatbot.dev / iLink 这一条产品路径。

- [ ] 新增 `PlatformWeChatBot = "wechatbot"`，同步 `internal/channel/types.go` 与 `internal/imctx/context.go`。
- [ ] 删除 `PlatformWeChatWechaty`、`PlatformWeChatPadPro` 及相关旧微信 platform enum。
- [ ] 删除 `internal/channel/wechat/wechaty/`。
- [ ] 删除 `internal/channel/wechat/wechatpadpro/`。
- [ ] 清理 `internal/bootstrap/server.go` / `internal/bootstrap/helpers.go` 中旧协议 import、初始化、注册和 reload 分支。
- [ ] **(Eng v0.2 / Arch-2 修正)** 删除 `BuildReloadChannelFunc` 中的 wechaty / wechatpadpro 分支；不要删除整个 `BuildReloadChannelFunc`，因为飞书 reload 仍依赖它。删除 `internal/api/config_handlers.go` 的 `handleReloadWeChatProtocol` 路由；运行中的 server 调用旧微信 reload 路径必须 404 或明确 unsupported，不允许 panic。
- [ ] 清理 `internal/api/config_handlers.go` 中 WeChatPadPro / Wechaty 配置状态、更新、reload 接口。
- [ ] 清理 `internal/config/config.go` 中旧协议配置结构；新增官方 `channel.wechatbot.enabled` 全局开关。
- [ ] 清理 `internal/tools/sendim.go` 中旧微信 platform enum。
- [ ] `channel.InboundMessage` / `channel.OutboundMessage` 增加 `OwnerUserID string`；该字段只表示 user-scoped IM scope，wechatbot 必填，飞书 / 钉钉 / 企微为空。
- [ ] `internal/imctx` 新增 `SendRequest`，作为 tools / API / Router 共用的出站 DTO；`internal/tools` 禁止 import `internal/channel`。
- [ ] `channel.Binding` 增加 `TenantKey string`；`Bind` / `LookupSession` / `Unbind` 内部 key 改为 `platform:tenantKey:chatID`。
- [ ] `internal/channel/router.go` 新增 `OwnerUserResolver` 注入点；`processMessageImpl` 在 `OwnerUserID != ""` 时解析 owner user、注入 `auth.WithUser`、跳过 `enrichCtx`，在 `OwnerUserID == ""` 时保持现有 `enrichCtx` 行为。
- [ ] `internal/tools/sendim.go` 的 `IMRouter.SendMessage` 改为接收 `imctx.SendRequest`；工具 schema 不增加 `owner_user_id`，wechatbot owner 只能从当前 auth user 派生。
- [ ] 增加统一接口 regression：`TestRouter_UserScopedSkipsEnrichCtx`、`TestRouter_UserScopedInjectsOwnerUser`、`TestRouter_TenantScopedEnrichCtxUnchanged`、`TestBindingKeyIncludesTenantKey`、`TestSendim_DoesNotAcceptOwnerUserIDFromAgent`。
- [ ] **(Eng v0.2 / CQ-2)** Migration 幂等性 golden test：`TestWeChatMigration_RunTwice_Idempotent`——连续跑两次 migration 不报错；任何 ALTER 必须 `IF NOT EXISTS`，任何 CREATE INDEX 必须 `IF NOT EXISTS`。
- [ ] 清理或改写 `internal/tools/wechat_ops.go`：旧协议专属工具不得继续暴露为可用能力。
- [ ] 删除前端旧 `WeChatSettings.tsx`，并从 `AdminSettings.tsx` integrations 中移除。
- [ ] 清理旧协议 i18n、文档、测试和配置示例。
- [ ] 增加 session ID 测试：`BuildSessionID(wechatbot, ownerUserID, peerWxid)`。
- [ ] 增加 `user_external_ids`、`wechat_conversations` additive migration。
- [ ] 定义 `wechatbot.Backend` / `BotRegistry` / `wechatbot.Plugin` / `WeChatConnectionService` 接口，不接 SDK 也能写单测；不得把 BotRegistry 暴露为 Master / tools / session API 的发送依赖。
- [ ] 定义 wechatbot 状态枚举和 API response types。

验收：

- `rg -n "wechatpadpro|wechaty|wechat-wechatpadpro|wechat-wechaty" internal frontend/src config.example.json docs` 只允许命中历史说明或本计划中的清理项。
- `go test ./internal/imctx ./internal/channel ./internal/api ./internal/tools -v`
- 无 `im-wechatbot-{wxid}` 这类手拼模板进入代码。

### Phase 1：后端连接与入站闭环

目标：用户扫码后，微信消息能进入属于该用户的 Agent session。

- [ ] 接入 `github.com/corespeed-io/wechatbot/golang`。
- [ ] 实现 `BotInstance` 生命周期：login、run、stop、panic recover、状态更新。
- [ ] **(Eng v0.2 / Arch-4)** BotInstance panic 自愈策略：panic recover 后 status 转 `error` → 30s 后自动 `Login(force=false)` → 连续 3 次失败转 `relogin_required` 等用户手动操作；新增 Prometheus 指标 `wechatbot_auto_recover_total{result}`。
- [ ] 实现 `wechatbot.Plugin`：满足 `channel.ChannelPlugin`，`Platform() == "wechatbot"`，`Send(ctx, OutboundMessage)` 内部按 `OwnerUserID` 找 BotInstance。
- [ ] 实现 `BotRegistry`：按 `OwnerUserID` 管理 BotInstance，只服务 wechatbot 插件与 `/api/v1/wechat/*` 状态 API。
- [ ] **(Eng v0.2 / Perf-1 修正)** BotRegistry 选择并发结构前先定义锁边界：不得持锁调用 SDK / 网络 / store。默认采用 `map + sync.RWMutex`；只有基准或复杂度证明有必要时才使用 `sync.Map`。
- [ ] 实现 `/api/v1/wechat/status/login/events/logout/relogin`。
- [ ] **(Eng v0.2 / Arch-1 修正)** WS 鉴权统一走 `Sec-WebSocket-Protocol` header，不允许 URL query token；浏览器侧沿用现有契约 `new WebSocket(url, [\`bearer-${token}\`, 'v1'])`，后端按 `bearer-` 前缀解析 JWT。
- [ ] `BotInstance.handleIncoming` 必须调用现有 `Router.HandleMessage(ctx, InboundMessage{OwnerUserID, TenantKey: OwnerUserID, ...})`；禁止新增 `HandleMessageWithOwnerContext` 旁路入口。
- [ ] 入站时 upsert `wechat_conversations`。
- [ ] 入站时用 owner user context 创建/处理 session。
- [ ] 对 SDK QR、scanned、expired、error 事件做 Web 事件推送。
- [ ] 凭证目录按用户隔离，权限符合要求（0700 / 0600）。
- [ ] **(Eng v0.2 / CQ-3)** 凭证目录权限单测 `TestWeChatBotCredentialsDirPermission`：BotInstance 创建后 `os.Stat` 验证目录 0700、`credentials.json` 0600。

验收：

- 单用户扫码成功。
- 微信联系人发文本消息后，DB 出现正确 `session_id` 和 `owner_user_id`。
- Master 处理时 user context 是 OwnerUserID，不是 PeerWxid。
- 断开/重启后不会产生重复 BotInstance。

### Phase 2：Web 会话体验与双向发送

目标：微信会话成为 Web 上的一等会话。

- [ ] 注册 `/settings` 路由并加入主侧边栏。
- [ ] 在 `Settings.tsx` 加入 `WeChatConnectionPanel`。
- [ ] `GET /api/v1/sessions` 支持展示当前用户自己的 `wechatbot` sessions。
- [ ] Chat 页面识别 `wechatbot` session。
- [ ] `POST /api/v1/sessions/{id}/messages` 对 `wechatbot` session 构造 `imctx.SendRequest` 并走 Router 统一出站，不直连 BotRegistry。
- [ ] 出站成功/失败写入消息记录和广播事件。
- [ ] AdminSettings 只显示官方 wechatbot 全局开关；不得出现 WeChatPadPro / Wechaty。

验收：

- 用户 A 的微信会话不出现在用户 B 列表。
- 用户在 Web 回复有 context 的微信会话，微信端能收到。
- 无 context 时返回清晰 409，不调用 Master 伪装成功。
- `npm run build` 和 `npm run lint` 通过。

### Phase 3：工具、媒体与可观测性

目标：让 Agent 工具和媒体体验不再割裂。

- [ ] `send_im_message` 支持 user-scoped wechatbot；不新增 `wechat_send_message`，避免工具层分裂。
- [ ] 官方 wechatbot 不支持的旧微信 ops 工具从注册表移除，或统一返回“官方协议不支持该能力”。
- [ ] 图片/语音/文件/视频入站占位展示。
- [ ] 可选实现媒体下载与预览。
- [ ] 添加 metrics：登录状态、入站/出站计数、SDK 错误、重连次数。
- [ ] 日志对 wxid 做 hash 或脱敏。

验收：

- Agent 无法跨用户发送微信消息。
- 媒体消息不会导致入站 handler panic。
- metrics 不使用高基数 `user_id` / 原始 `wxid` label。

### Phase 4：上线硬化

目标：官方 wechatbot 链路上线后补齐生产稳定性，不再包含旧协议清理任务，因为旧协议已在 Phase 0 清空。

- [ ] 登录失败、掉线、重连风暴的运行手册。
- [ ] 凭证目录巡检脚本。
- [ ] SDK 错误码分类与用户提示表。
- [ ] 生产环境 canary：扫码、入站、Web 出站、logout。
- [ ] 删除旧协议的最终 grep gate 纳入 CI。

### Phase 5：后续增强

不进 MVP：

- 群聊完整支持。
- 联系人全量同步。
- 任意 wxid 冷启动推送。
- 朋友圈、群管理、好友管理。
- 多微信账号绑定。
- 端到端加密。

这些能力要基于真实用户反馈再排优先级。

---

## 12. 安全与隐私

### 12.1 权限边界

- 所有 `/api/v1/wechat/*` 接口必须要求登录。
- Admin 不默认拥有普通用户微信会话访问权；沿用当前 session owner 模型。
- Web 出站必须验证 `session.user_id == 当前用户`。
- Tool 出站必须从 context 解析当前用户，不能让模型自报 owner。
- Event hub 必须按 user 分区。

### 12.2 日志与指标

禁止：

- 打印二维码原始登录凭证。
- 打印 credentials。
- 在 metrics label 中使用原始 `wxid`、`user_id`、消息内容。
- 在错误里返回 SDK token / context token。

允许：

- 日志中使用 session_id。
- 对 `wxid` 做 hash 后用于 debug。
- 记录 `content_len`，不记录全文。

### 12.3 威胁模型测试

必须覆盖：

- 用户 A 不能订阅用户 B 的 QR 事件。
- 用户 A 不能通过 session id 给用户 B 的 PeerWxid 发消息。
- 同一个 `OwnerAccountID` 不能被两个 `OwnerUserID` 同时绑定。
- BotInstance panic 不影响其他用户 Bot。
- logout 不清理其他用户凭证目录。

---

## 13. 可观测性

建议指标：

| Metric | 类型 | Labels |
| --- | --- | --- |
| `wechatbot_active_bots` | gauge | none |
| `wechatbot_login_total` | counter | `status` |
| `wechatbot_messages_inbound_total` | counter | `msg_type` |
| `wechatbot_messages_outbound_total` | counter | `status` |
| `wechatbot_send_unavailable_total` | counter | `reason` |
| `wechatbot_sdk_errors_total` | counter | `error_type` |
| `wechatbot_reconnect_total` | counter | none |

告警建议：

- `wechatbot_active_bots` 突然归零。
- `wechatbot_sdk_errors_total` 短时间激增。
- `wechatbot_login_total{status="failure"}` 连续失败。
- `wechatbot_reconnect_total` 短时间异常升高。

---

## 14. 验收测试清单

后端建议测试：

**核心 (10 项，Phase 1-2 验收门)：**

- `TestWeChatOwnerPeerRouting`：入站消息使用 OwnerUserID 建 session。
- `TestWeChatDoesNotEnrichPeerWxidAsUser`：PeerWxid 不会进入 auth user 查找。
- `TestWeChatSessionIDUsesOwnerUserID`：session id 包含 owner 维度且通过 `BuildSessionID`。
- `TestWeChatConversationUpsertIdempotent`：同一 owner + peer 重复入站不建重复会话。
- `TestWeChatWSUserIsolation`：事件只发给当前用户。
- `TestBindExternalIDConflict409`：同一微信账号二次绑定返回冲突。
- `TestWebSendToWechatRejectsCrossUser`：跨用户 session 出站被拒绝。
- `TestWebSendRequiresContextToken`：无 context token 返回 409。
- `TestBotRegistryShutdownTimeout`：停止 Bot 有超时保护。
- `TestBotRunPanicRecover`：单个 Bot panic 被捕获并更新状态。

**v0.4 必加 Regression 测试（IRON RULE，覆盖统一 IM scope 改造）：**

- `TestRouter_UserScopedInboundSkipsEnrichCtx`：`OwnerUserID != ""` 时 PeerWxid 永不当 system user 反查。
- `TestRouter_UserScopedInboundInjectsOwnerUser`：Router 通过 OwnerUserResolver 注入 owner user。
- `TestRouter_TenantScopedEnrichCtxUnchanged`：飞书 / 钉钉 / 企微现有 sender 反查行为保持不变。
- `TestRouter_UserScopedInboundUsesSharedDedup`：同一个 `HandleMessage` 链路共用现有 dedupBackend，1 条消息只处理一次。
- `TestBindingKeyIncludesTenantKey`：同一平台同一 chatID 在不同 tenant/owner 下绑定到不同 session。
- `TestSendim_DoesNotAcceptOwnerUserIDFromAgent`：工具 schema 不接受 Agent 传 owner_user_id。
- `TestSendim_WechatbotDerivesOwnerFromAuthContext`：wechatbot 出站 owner 来自 auth context。
- `TestSendim_TenantScopedPlatformsKeepOwnerEmpty`：企业 IM 出站保持 `OwnerUserID=""`。
- `TestBuildSessionID_WechatbotThreeSegments`：`im-wechatbot-{ownerUserID}-{peerWxid}` 格式。
- `TestSessionList_WechatbotVisibleOnlyForOwner`：只展示当前用户的 wechatbot session。
- `TestSessionList_OtherImSessionsStillFiltered`：其他 IM session 仍被 `im-` 前缀过滤。
- `TestSessionSend_WechatbotRoutesThroughRouter`：wechatbot session 走 Router 统一出站，不进 Master，不直连 BotRegistry。
- `TestSessionSend_NonWechatbotImSessionStillBlockedFromMaster`：非 wechatbot IM session 行为不变。

**Eng v0.2 必加 4 项 Critical Failure Mode 测试（防 silent failure）：**

- `TestBotRegistry_ConcurrentAccess_NoDeadlock`：100 goroutine 并发 Get/Send/Stop 不死锁（F3）。
- `TestWeChatBotCredentialsDirPermission`：目录 0700、文件 0600（F5）。
- `TestWeChatMigration_RunTwice_Idempotent`：migration 重跑不报错（F6）。
- `TestSendIm_CrossOwnerRejected`：Agent 用 user A 的 session 给 user B 的 PeerWxid 发，工具层拒绝（F8）。

前端建议验证：

- `/settings` 可访问。
- 未连接、等待扫码、已扫码、在线、掉线、错误状态均可展示。
- 二维码过期后可重新获取。
- 微信 session 在列表中显示来源标识。
- `can_send=false` 的会话给出明确提示。
- `npm run lint`。
- `npm run build`。

全链路手工验收：

1. 用户 A 连接微信。
2. 用户 B 连接另一个微信或保持未连接。
3. 外部联系人向用户 A 微信发消息。
4. 用户 A Web 看到会话。
5. 用户 B Web 看不到该会话。
6. 用户 A Web 回复，微信端收到。
7. 用户 A logout 后，Web 出站返回未连接。

---

## 15. 回滚策略

### 15.1 Feature flag

新增配置：

```json
{
  "channel": {
    "wechatbot": {
      "enabled": false
    }
  }
}
```

语义：

- `enabled=false` 时隐藏用户扫码入口。
- 已运行 BotInstance 应优雅停止。
- API 返回 `disabled` 状态。
- 不删除数据。

### 15.2 数据库

- Phase 0-3 只添加表/字段/索引。
- 旧协议如果只有配置字段和本地凭证目录，Phase 0 可以清理；如发现真实业务数据表，先迁出或备份后再删除。
- 旧 WeChatPadPro / Wechaty 凭证可以删除，但必须限定到旧协议目录，不能误删 `{DataDir}/wechatbot/users/*`。
- rollback 时停用 wechatbot flag 即可。

### 15.3 代码

如果 Phase 1/2 出现严重问题：

- 关闭 `channel.wechatbot.enabled`。
- 不回滚到 WeChatPadPro / Wechaty。
- Session 和 conversation 数据保留，修复官方链路后继续使用。

---

## 16. 文件级改造清单

### 必改

| 文件 | 改动 |
| --- | --- |
| `internal/channel/types.go` | 新增 `PlatformWeChatBot` |
| `internal/imctx/context.go` | 新增 `PlatformWeChatBot` |
| `internal/imctx/session_test.go` | 增加 wechatbot session id 测试 |
| `internal/channel/wechatbot/*` | 新增 SDK adapter 与 BotInstance |
| `internal/channel/wechatbot/plugin.go` | 新增 wechatbot ChannelPlugin |
| `internal/channel/wechatbot/registry.go` | 新增插件内部 BotRegistry |
| `internal/api/wechat_handlers.go` | 新增用户级微信 API |
| `internal/api/session_handlers.go` | 微信 session 列表展示与 Web 出站分支 |
| `internal/tools/sendim.go` | 增加 user-scoped wechatbot 出站能力 |
| `frontend/src/App.tsx` | 注册 `/settings` |
| `frontend/src/pages/Settings.tsx` | 加入微信连接区域 |
| `frontend/src/api/wechat.ts` | 新增前端 API client |
| `frontend/src/hooks/useWechatConnection.ts` | 新增状态与事件 hook |
| `frontend/src/components/settings/WeChatConnectionPanel.tsx` | 新增个人微信连接 UI |

### 必删/清空

| 文件/目录 | 改动 |
| --- | --- |
| `internal/channel/wechat/wechaty/` | 删除 gRPC 旧协议实现 |
| `internal/channel/wechat/wechatpadpro/` | 删除 iPad 旧协议实现 |
| `frontend/src/components/settings/WeChatSettings.tsx` | 删除旧协议 Admin 配置组件 |
| `internal/api/config_handlers.go` | 清理旧微信协议配置接口；保留非微信配置能力 |
| `frontend/src/types/api.ts` | 删除旧微信协议类型字段 |
| `config.example.json` | 删除旧微信协议配置示例，新增 `wechatbot` |

---

## 17. 非目标

本计划明确不做：

- 把微信外部联系人注册成 agents-hive 用户。
- 让管理员代替普通用户扫码。
- 给任意 wxid 冷启动推送。
- **完整群聊支持（SDK 当前完全不支持，等上游 iLink 协议支持后另议）**。
- **企业全员场景（SDK 不支持群聊导致无法覆盖；走企微通道平行立项）**。
- **@ 提及 / 消息撤回 / 群成员管理 / 朋友圈 / 好友管理 / 多账号绑定 / 端到端加密**（SDK 均不支持）。
- 把 WeChatPadPro / Wechaty 作为官方链路失败后的回退方案。
- 改造整个 auth provider 模型。
- 顺手开放所有 IM session 到前端。

---

## 18. 最终产品判断

这次重构的产品价值不在“支持了新的微信 SDK”，而在“微信联系人变成用户自己的 Agent 会话”。

因此优先级必须保持：

1. 身份与会话归属正确。
2. 用户扫码和状态反馈清楚。
3. 微信入站到 Web 可见。
4. Web 能对已有上下文发回微信。
5. 工具和媒体逐步增强。
6. 旧协议在 Phase 0 清空，后续不再回头兼容。

只要 `OwnerUserID / OwnerAccountID / PeerWxid` 三者边界不乱，后续功能都能自然增长；一旦这三个身份混在一起，系统会在权限、会话、发送和审计上同时失控。

---

## 19. CEO + Eng 联合评审记录（2026-05-10）

**审核结论：HOLD SCOPE，4 项硬决策已锁定，4 项风险已记录。**

### 关键修订（已落到对应章节）

| # | 决策项 | 选择 | 影响章节 |
|---|---|---|---|
| D1 | 产品定位 | 销售/老板个人助手（付费个人或企业采购分发个人） | 头部锁定区 + 0 节 + 17 节 |
| D2 | 企微通道关系 | 平行优先项单独立项，本计划范围不变 | 头部锁定区 |
| D3 | 旧协议处理 | Phase -1 Spike 通过后，Phase 0 硬删除 WeChatPadPro / Wechaty | 头部锁定区 + 11 节 |
| D4 | 统一 IM scope | `OwnerUserID` 作为 `InboundMessage` / `OutboundMessage` 显式字段流经同一个 Router；不新增 owner 专用入口，不用 context value，不改 `enrichCtx` 签名 | 6.3 / 6.4 / 8.1.1 / 11 节 |

### 群聊能力硬事实（实证）

基于官方 Go SDK / iLink 文档，当前没有可依赖的群聊入口：
- 未暴露稳定 RoomID/ChatroomID/GroupID 入站模型
- 未暴露稳定 SendToRoom / 群发 API
- 未暴露 @ 提及 / 群成员管理能力

**MVP 真实可用场景：1:1 客户咨询补充。**不要在文档中写死市场覆盖比例，除非另有业务数据来源。

### 蓝军记录的 4 项风险（接受但需后续监控）

| # | 风险 | 缓解 |
|---|---|---|
| R1 | wechatbot.dev SDK SLA / 倒闭 / 封号风险，one-way door | Phase 4 加 canary + 上线 1 个月 hardening；监控 `wechatbot_sdk_errors_total` 指标突增告警 |
| R2 | wechatbot owner 注入污染现有 IM 用户反查接口 | owner 只走 `InboundMessage.OwnerUserID` / `OutboundMessage.OwnerUserID`；Router 看到 `OwnerUserID != ""` 时走 `OwnerUserResolver` 并跳过 `enrichCtx`，企业 IM 继续走现有 sender 反查 |
| R3 | 个人微信封号导致员工私人社交关系损失 | 0 节明确为"销售/老板自用"，企业全员场景显式不支持；产品文案警示扫码风险 |
| R4 | 跨计划优先级冲突（Memory / Multi-agent / 微信 / 企微 / 多业务平台化 / 定时任务）| Phase -1 / Phase 0 启动前由产品/技术联合排期；不要把 Memory 重构作为微信 Phase 0 的硬依赖 |

### 跨计划集成点（v0.2 新增）

- **Memory RuntimeContext**：wechatbot 入站可以提供 `TaskType="im_wechat_chat"` 这类 IM 上下文，但当前 Master 会在 ReAct 入口重写 `memoryRuntimeContext(..., "react")`。因此要么让 Master merge 现有 RuntimeContext，要么把该集成列为后续增强，不能只靠 BotInstance 塞 context。
- **Multi-agent 工作台**：微信入站默认走 Master ReAct（轻路径），不进 Planner。除非用户消息触发"做个 X"等关键词由 Master 路由器判定为重任务。
- **企微平行计划**：本计划完成后启动企微通道补齐（群聊、合规、企业场景）。两条通道共享统一 IM Router、tenant-aware binding key 与 session 展示策略，不共享个人微信 owner 语义。

### Phase -1 SDK Spike（强制前置）

Phase 0 启动前必须做 1-day spike：
1. 拉 `github.com/corespeed-io/wechatbot/golang` 跑 hello world
2. 实测扫码登录耗时（计划承诺 30s 内）
3. 实测 `Send` 在无 context_token 时的具体错误码
4. 实测 `OnMessage` 回调的 goroutine 模型（是否 SDK 内单 goroutine 串行？panic 隔离？）
5. 实测多用户多 BotInstance 并发是否有共享状态污染

Spike 通过后再进 Phase 0；Spike 失败则计划暂停，重新评估。

### 测试清单补全（基于评审）

在原 14 节基础上必须新增：
- `TestWeChatOwnerContextInjected`：wechatbot 入站时系统用户来自 OwnerUserID，而不是 PeerWxid
- `TestFeishuExistingEnrichCtxUnchanged`：飞书现有 sender 反查行为保持不变
- `TestBotInstancePanicRecover`：单 Bot panic 不影响其他用户 Bot
- `TestConcurrentLoginIdempotent`：同一用户并发 login 不创建重复 BotInstance
- `TestRouterEnrichCtxNotCalledWithPeerWxid`：防止 PeerWxid 被误当作 ownerUserID 反查

### 后续门禁

- Phase -1 SDK Spike 不通过禁止启动 Phase 0
- Phase 1 关键路径测试必须覆盖 owner routing、WS isolation、send context、panic recover、cross-owner deny；覆盖率目标可作为质量信号，不作为唯一门禁
- Section 14 列出的 v0.4 regression 测试 + 4 条 critical failure mode 测试 100% 通过才能进 Phase 2
- 线上 canary 监控指标 `wechatbot_sdk_errors_total` / `wechatbot_auto_recover_total` 阈值告警必须在 Phase 4 上线前接通

---

## 20. Eng v0.2 工程修订批次（2026-05-10）

本批次基于 /plan-eng-review 深度工程审核，**已就地落入 Phase 0 / Phase 1 / Section 14**，无需单独追踪。

| 编号 | 修订项 | 落点 |
|---|---|---|
| Arch-1 | WS 鉴权统一 `Sec-WebSocket-Protocol` header，使用现有 `bearer-{jwt}` + `v1` 子协议契约 | Phase 1 工作项 |
| Arch-2 | Phase 0 删除旧微信 reload API 与 `BuildReloadChannelFunc` 中的旧微信分支，保留飞书 reload | Phase 0 工作项 |
| Arch-3 | 不新增 `HandleMessageWithOwnerContext`；`OwnerUserID` 作为统一 `InboundMessage` 字段复用现有 `HandleMessage`、dedup、debounce、retry_queue | Phase 0/1 工作项 |
| Arch-4 | BotInstance panic 自愈策略（30s × 3 / 转 manual） | Phase 1 工作项 |
| CQ-1 | sendim 改为统一 `imctx.SendRequest`；owner 从 auth context 派生，禁止 Agent 传 owner_user_id | Phase 0/1 工作项 + Section 14 |
| CQ-2 | Migration 幂等性 golden test | Phase 0 工作项 + Section 14 |
| CQ-3 | 凭证目录权限单测 0700/0600 | Phase 1 工作项 + Section 14 |
| Perf-1 | BotRegistry 明确锁边界；默认 `map + sync.RWMutex`，不得持锁调用 SDK，必要时用 benchmark 决定是否 `sync.Map` | Phase 1 工作项 |

**测试 Coverage Diagram：** 56 paths / 0% 覆盖 / **13 regression + 12 E2E + 4 critical failure mode 必须补**。详见 `~/.gstack/workspace/company/guoss-qa_up-eng-review-test-plan-20260510-204432.md`。

**失败模式注册表（8 项，4 项 CRITICAL gap）：**

| 编号 | 路径 | 失败 | RESCUED | TEST | 用户感知 |
|---|---|---|---|---|---|
| F1 | BotInstance.Run goroutine | SDK panic | NEW | TestBotRunPanicRecover | "微信连接断开" |
| F2 | Router scope 字段 | owner 注入绕过 retry_queue / dedup | 复用现有 `HandleMessage` 链路 | TestRouter_UserScopedInboundUsesSharedDedup | 消息丢 |
| F3 ⚠️ | BotRegistry map | 持锁调用 SDK 导致阻塞/死锁 | 锁内只做 map 读写，SDK 调用在锁外 | **TestBotRegistry_ConcurrentAccess_NoDeadlock** | 全用户卡死 |
| F4 | bot.Send | 无 context_token | 按错误码 | TestWebSendRequiresContextToken | 409 提示 |
| F5 ⚠️ | 凭证目录 | chmod 破坏 | N | **TestWeChatBotCredentialsDirPermission** | "凭证不可读" |
| F6 ⚠️ | DB 迁移 | 重跑 ALTER 崩 | IF NOT EXISTS | **TestWeChatMigration_RunTwice_Idempotent** | server 启动失败 |
| F7 | WS 事件 hub | 跨用户泄漏 | 已计划 | TestWeChatWSUserIsolation | 隐私事故 |
| F8 ⚠️ | send_im_message | 跨 owner 越权 | N | **TestSendIm_CrossOwnerRejected** | Agent 给陌生人发消息 |

**Worktree 并行：** 4-6 周 → 并行后 3-4 周（Lane A 主线 / Lane B 数据库迁移独立 / Lane C 前端独立 / Lane D session_handlers）。Conflict flag：Phase 0a 和 Phase 0b 都改 `internal/channel/router.go`，必须 0a 严格先于 0b。
