{
  "_comment": "session-scope-regression-matrix Phase 4.1 — CI harness 专用 bootstrap config。",
  "_comment_scope": "本文件只给 CI e2e workflow 用；本地跑 `go test ./tests/regression/...` 不依赖此文件。",
  "_comment_no_external": "禁用所有外部依赖：LLM provider / 飞书 longconn / remote MCP。session-scope regression 只测 eventBus envelope + WS filter 两层契约，不需要上游 provider。",

  "server": {
    "port": 18081,
    "host": "127.0.0.1",
    "cors_origins": ["http://localhost:5173"]
  },

  "logging": {
    "level": "debug",
    "format": "json",
    "file": "/tmp/hive-ci.log",
    "console_level": "error",
    "max_size": 10,
    "max_backups": 1,
    "max_age": 1
  },

  "gateway": {
    "enabled": false,
    "tokens": []
  },

  "store": {
    "_comment": "CI 里 services.postgres 绑 5432；TEST_DATABASE_URL env 覆盖也可",
    "type": "postgres",
    "postgres": {
      "dsn": "",
      "host": "localhost",
      "port": 5432,
      "database": "hive_ci",
      "user": "hive",
      "password": "ci",
      "ssl_mode": "disable",
      "max_conns": 5
    }
  }
}
