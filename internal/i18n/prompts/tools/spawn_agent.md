## spawn_agent 使用规范

spawn_agent 是同步的：调用后等待子 Agent 完成才返回结果。

### 何时使用（仅在以下场景）
- 需要真正并行的独立子任务（如同时分析 3 个独立模块）
- 需要强隔离的 system prompt（与主任务完全不同的角色）
- 需要临时缩小工具集（安全沙箱场景）
- 探索性任务可以用 task 工具委派给 explore Agent（只读，快速）

### 使用约束
- 最多同时 3 个子代理
- 子代理不能再 spawn 子代理
- 简单任务不要用 spawn_agent，直接执行更快
