# 认知增强二期 A 现状审计（2026-04-20）

> 审计触发：owner 让我"立即启动二期 A"。开工前做现状扫描，发现 tasks.md 和实际代码严重脱节。
>
> 底层逻辑：加固阶段（harden-spec-driven-phase2）实施时切换了存储架构，老 tasks.md 没跟上。

## 二期 A 九条任务逐条审计

### 已完成（代码侧，测试侧部分）

| 任务 | 状态 | 证据 |
|------|------|------|
| 2.0 TaskPlanning 常量 + 路由 | ✅ | `internal/airouter/types.go:25` + `selector.go:41-51` + `selector_test.go` 5 测试 |
| 2.1 schema 骨架 | ✅（等价） | `internal/specdriven/types.go` 定义 SessionSpecState / ChangeRef / Context / PlanStep / Decision。Proposal/Task 对应 Postgres 表字段，Go 侧不需单独 struct |
| 2.4 continuation 模糊匹配 | ✅ | `internal/specdriven/continuation/resolver.go` + `resolver_test.go` |
| 2.6 planner | ✅ | `internal/specdriven/planner/plan.go` + `decode.go` + 两个 _test.go |

### 被 Phase 2 架构演进废弃

| 任务 | 被替代者 | 证据 |
|------|---------|------|
| 2.2 store.go 文件系统后端 | Postgres `hive_spec_changes` + `SpecChangeStore` | `internal/store/spec_store.go`；README.md 第 34-39 行："canonical stays in Postgres" |
| 2.3 retention + blobstore gzip | `SpecChangeStore.RetentionSweep` | `internal/store/spec_store_test.go:306-340` TestSpecStore_RetentionSweep_DeletesOldCompleted |

### 真空缺口（需 owner 决策）

| 任务 | 现状 | 判断 |
|------|------|------|
| 2.5 complexity 启发式 | 零实现、零调用点 | 可能是老设计残留——当前用 `intake.Mode` 开关（legacy/dual/spec）做决策，没有复杂度评分 |
| 2.7 verifier | 零实现、零调用点 | "tasks 全 check" 由 session_loop 做；"archive 迁移" 由 `openspec archive` CLI 做——verifier 的职责被工具链吸收 |
| 2.8 单测 ≥ 8 case | 部分完成 | 现有 resolver_test / plan_test / decode_test / decide_test 都是 _test 前缀，条数需逐个核查 |

## 三个拍板项

### 决策一：2.2 / 2.3 是否正式标记 OBSOLETE？
- 选甲：更新 tasks.md，把 2.2 / 2.3 注记"by Phase 2 D3 (Postgres canonical)"→ 二期 A 颗粒度缩小
- 选乙：保留原任务描述，另外加一条"已由 Phase 2 覆盖"注释

**推甲**——文档要反映真相，owner 看 tasks.md 不该再看到已废弃项

### 决策二：2.5 complexity + 2.7 verifier 做不做？
- 选甲：标记 OBSOLETE（架构已演进，无调用点 = 无需求）
- 选乙：补齐实现，即使现在没调用点，保留为未来 hook
- 选丙：重新设计——如果有新的使用场景，在本变更里补 design 说明

**推甲**——YAGNI。有需求再加，没需求就别占空间

### 决策三：二期 A 按真实进度收尾
- 二期 A 原 9 条 → 采纳决策一二后 → 变成 4 条已完成（2.0/2.1/2.4/2.6/2.8）+ 2 条 OBSOLETE（2.2/2.3）+ 2 条 OBSOLETE（2.5/2.7）
- 结论：**二期 A 实际可宣告完成**，进入二期 B（主控层接入）

## 给 owner 的行动

**5 秒决策**：是否同意甲/甲/甲？
- 同意 → 我更新 tasks.md + 更新 SPLIT_RECOMMENDATION.md，标注二期 A 已完，二期 B 可排期
- 不同意 → 请说明哪一条保留 + 为什么，我按您的判断走
